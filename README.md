# IronPath

Fitness smart mirror MVP with MoveNet squat tracking.

## Setup

- npm install
- npm run dev

Note for running "Start Workout": MoveNet may take a minute or two to load while the pose model downloads and initializes.
